import React from "react";
import styled from "styled-components";

const Container = styled.div``;

const AppoinmentDetails = ({ children }) => {
  return <Container></Container>;
};

export default AppoinmentDetails;
